# Environment files - only .env.example is tracked
.env
.env.*
!.env.example

# OS noise
.DS_Store
Thumbs.db

# Python bytecode (scripts/ has no per-directory .gitignore of its own -
# apps/api/ and packages/sdk-py/ each have one already)
__pycache__/
*.pyc

# Editors
.idea/
.vscode/
